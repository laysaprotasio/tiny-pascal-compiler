var x, y: integer; flag: boolean;

procedure MostraSoma(a: integer; b: integer);
begin
    x := a + b;
    if x > 10 then
        y := x
    else
        y := 0;
end;

function Soma(a: integer; b: integer): integer;
begin
    x := a + b;
end;

begin
    x := 5;
    y := 2;
    flag := true;
    if x < 10 then
        y := x + y
    else
        y := 0;
    Soma(x, y);
    MostraSoma(x, y);
end.